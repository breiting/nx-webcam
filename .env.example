# Kamera-Params
CAM_DEVICE=/dev/video0
CAM_WIDTH=1280
CAM_HEIGHT=720
CAM_FPS=15

# Falls H.264-only, nutze GStreamer-Pipeline (Beispiel):
# GST_PIPE=v4l2src device=/dev/video0 ! video/x-h264,framerate=15/1 ! h264parse ! avdec_h264 ! videoconvert ! appsink

# Prusa Connect (Snapshot-Upload) – entweder URL+Token ...
PRUSA_URL=
PRUSA_TOKEN=
PUSH_EVERY=5

# ... ODER lass PRUSA_URL leer und trage http://<host>:8000/snapshot.jpg in Prusa Connect als Snapshot-URL ein.
